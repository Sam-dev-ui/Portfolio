<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rupender Singh - Introductory Website</title>
  <link rel="stylesheet" href="styles.css"> <!-- Link to your CSS file -->
  <style>
    /* Header Styles */
    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 20px;
      background-color: #2D2D2D; /* Dark background */
      color: #FFFFFF; /* White text */
    }

    .header-left {
      font-size: 1.5rem;
      font-weight: bold;
    }

    .header-right {
      display: flex;
      align-items: center;
    }

    .header-right a {
      color: #FFFFFF;
      margin-left: 15px;
      text-decoration: none;
      font-size: 1rem;
      display: flex;
      align-items: center;
    }

    .header-right a:hover {
      color: #FF6F61; /* Hover color */
    }

    .header-right .contact-info {
      display: flex;
      align-items: center;
      margin-right: 15px;
    }

    .header-right .contact-info i {
      margin-right: 8px;
    }

    .get-in-touch-button {
      background-color: #FF6F61;
      color: #FFFFFF;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      text-decoration: none;
      font-weight: bold;
    }

    .get-in-touch-button:hover {
      background-color: #E55B50; /* Darker hover color */
    }

    /* Description Section Style */
    section {
      padding: 60px 20px;
      background: #F5F5F5; /* Light background for contrast */
      color: #333333; /* Dark text color */
      margin: 20px 0; /* Margin for spacing */
      text-align: center; /* Center align the text */
    }

    .description-container {
      max-width: 800px;
      margin: 0 auto;
    }

    /* Typing Effect */
    .typing-container {
      display: inline-block;
      overflow: hidden;
      white-space: nowrap;
      border-right: 3px solid #FF6F61; /* Cursor effect */
      animation: typing 10s steps(60, end) 1s 1 normal both, blink-caret .75s step-end infinite;
      font-family: 'Roboto', sans-serif;
      font-size: 1.1rem;
      line-height: 1.6;
    }

    .typing-container p {
      margin: 0;
      display: inline;
    }

    /* Typing Animation Keyframes */
    @keyframes typing {
      from { width: 0; }
      to { width: 100%; }
    }

    @keyframes blink-caret {
      from, to { border-color: transparent; }
      50% { border-color: #FF6F61; }
    }
  </style>
</head>
<body>
  <!-- Header Section -->
  <header>
    <div class="header-left">Rupender Singh</div>
    <div class="header-right">
      <div class="contact-info">
        <a href="mailto:your.email@example.com"><i class="fas fa-envelope"></i> your.email@example.com</a>
        <a href="tel:+1234567890"><i class="fas fa-phone"></i> +123 456 7890</a>
      </div>
      <a href="https://wa.me/1234567890" class="get-in-touch-button">Get in Touch</a>
    </div>
  </header>

  <!-- About Me Section -->
  <section id="about">
    <div class="description-container">
      <div class="description-text">
        <div class="typing-container">
          <p>Hello! I'm Rupender Singh, a passionate digital marketer with a flair for innovative solutions. With a background in commerce and a knack for data-driven strategies, I specialize in optimizing SEO, managing projects, and delivering results. My goal is to help businesses thrive in the digital world through tailored marketing strategies and cutting-edge technology.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Font Awesome for Icons -->
  <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
</body>
</html>
